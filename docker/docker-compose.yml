# services:
#   web:
#     build:
#       context: ..
#       dockerfile: docker/Dockerfile
#     container_name: healthchecks
#     env_file:
#       - .env.example
#     # command:
#     #   - "uwsgi /opt/healthchecks/docker/uwsgi.ini"
#     ports:
#       - "8000:8000"
#       # To enable SMTP on port 2525, set SMTPD_PORT=2525 in .env
#       # and uncomment the following line:
#       # - "2525:2525"

#       #command: bash -c 'while !</dev/tcp/db/5432; do sleep 1; done; uwsgi /opt/healthchecks/docker/uwsgi.ini'

services:
  web:
    image: healthchecks1mg:1.1 # Use the pre-built image
    container_name: healthchecks
    env_file:
      - .env.example
    command: [ "uwsgi", "/opt/healthchecks/docker/uwsgi.ini" ]
    ports:
      - "8000:8000"
      # To enable SMTP on port 2525, set SMTPD_PORT=2525 in .env
      # and uncomment the following line:
      # - "2525:2525"
